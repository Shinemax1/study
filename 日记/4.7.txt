Require文件夹的时候，会先找package.json中的main键值，如果没有则找index.js

默认模版html文件要重新编译
默认布局layout下面的vue文件热更新的

Get的url大小最多时4k 由浏览器和服务器共同决定，主要由服务器决定
Post的请求正文大小由服务器决定，比如php和apache的配置文件中最多是上传5M的文件，如果要变动，需要修改参数
Head get的不完整版，发送请求后只返回响应头，不返回真正资源，主要测试服务器连没连通
Put 类似于post，post主要用于修改数据，put用于增加数据